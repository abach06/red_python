#Python packages
#Use "pip freeze" to see what version of packages are installed

#use "
#pip install boto3" to install boto 3

pip freeze > requirements.txt #creates a requirements file

pip install -r requirements.txt # will install the requirements file into your new environment